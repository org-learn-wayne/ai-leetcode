# ai-leetcode

This repo contains code generated from VS Code by running Codex extension with ChatGPT 5.4 model.

## Local REST API Sandbox

Run `run-movie-api.bat` from the repository root, then open `http://localhost:8080/` in a browser to try the Movie CRUD sandbox and the Healthcare Discussion Board APIs.
